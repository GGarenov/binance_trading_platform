# Copy to .env and fill in real values. .env is gitignored — never commit it.

# PostgreSQL connection string used by Prisma
DATABASE_URL="postgresql://cryptolab:cryptolab@localhost:5432/cryptostrategy_lab"

# Port the Express API listens on
PORT=4000

# No Binance API key needed for the MVP — public market data endpoints are keyless.
# When one is eventually added, it must be created with read-only permissions (never withdrawal).
